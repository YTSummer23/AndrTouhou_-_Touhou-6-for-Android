Make debug info output to window, not console. Except keyPressed number.
Fix movement of character. In different zones of window it moves with different speed.

Make const arrays instead of switchs.
Do something to mutexes(maybe delete it?). And do normal threads synchronization
Find where I can use Enums.
Write deleting of dynamic variables.
